# Simple Cloud LLM Pipeline
# Demonstrates a single-stage cloud pipeline

name: cloud-llm

input:
  type: text

stages:
  - id: llm
    model: gpt-4o-mini
    target: cloud
    provider: openai
    options:
      system_prompt: "You are a helpful assistant."
      max_tokens: 150
      temperature: 0.7
