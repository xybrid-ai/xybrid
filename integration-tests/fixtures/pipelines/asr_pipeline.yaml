# ASR Pipeline - Minimal Format
# Only `stages` is required. Everything else is optional or inferred.

stages:
  - wav2vec2-base-960h
  # Complete version:
  # - id: asr
  #   model: wav2vec2-base-960h
  #   version: "1.0"
  #   target: device
