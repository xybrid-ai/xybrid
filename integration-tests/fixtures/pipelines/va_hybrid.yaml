# Voice Assistant Pipeline (ASR -> LLM -> TTS)
# Demonstrates multi-stage pipeline with cloud integration

name: voice-assistant

stages:
  - whisper-tiny
  # Complete version:
  # - id: asr
  #   model: whisper-tiny
  #   target: device

  - model: gpt-4o-mini
    target: cloud
    provider: openai
    system_prompt: "You are a helpful voice assistant. Keep responses concise."
    max_tokens: 150
    temperature: 0.7
  # This is the complete version - cloud stages need explicit config

  - kokoro-82m
  # Complete version:
  # - id: tts
  #   model: kokoro-82m
  #   target: device
