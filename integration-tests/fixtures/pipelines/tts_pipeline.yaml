# TTS Pipeline - Minimal Format
# Only `stages` is required. Everything else is optional or inferred.

stages:
  - kokoro-82m
  # Complete version:
  # - id: tts
  #   model: kokoro-82m
  #   version: "0.1"
  #   target: device
