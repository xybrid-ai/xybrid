// <auto-generated>
// This file was generated by BoltFFI. Do not edit.
// </auto-generated>
#nullable enable

namespace XybridBolt
{
    public sealed class XybridDownload : global::System.IDisposable
    {
        private long handle;

        internal ulong Handle => unchecked((ulong)(ulong)global::System.Threading.Interlocked.Read(ref handle));

        internal XybridDownload(ulong handle)
        {
            if (handle == 0) throw new global::System.ArgumentException("handle must not be zero", nameof(handle));
            this.handle = unchecked((long)(ulong)handle);
        }


        /// <summary>
        /// Start downloading a registry model. Returns immediately.
        /// </summary>
        public static XybridDownload FromRegistry(string id)
        {
            WireWriter idWriter = new WireWriter();
            {
                idWriter.WriteString(id);
            }
            byte[] idBytes = idWriter.ToArray();
            ulong boltffiHandle = NativeMethods.NativeXybridDownloadFromRegistry(idBytes, (nuint)idBytes.Length);
            return boltffiHandle == 0 ? throw new global::System.InvalidOperationException("BoltFFI returned a null XybridDownload handle") : new XybridDownload(boltffiHandle);
        }



        /// <summary>
        /// Start downloading a registry model resolved for a specific platform.
        /// </summary>
        public static XybridDownload FromRegistryWithPlatform(string id, string platform)
        {
            WireWriter idWriter = new WireWriter();
            {
                idWriter.WriteString(id);
            }
            byte[] idBytes = idWriter.ToArray();
            WireWriter platformWriter = new WireWriter();
            {
                platformWriter.WriteString(platform);
            }
            byte[] platformBytes = platformWriter.ToArray();
            ulong boltffiHandle = NativeMethods.NativeXybridDownloadFromRegistryWithPlatform(idBytes, (nuint)idBytes.Length, platformBytes, (nuint)platformBytes.Length);
            return boltffiHandle == 0 ? throw new global::System.InvalidOperationException("BoltFFI returned a null XybridDownload handle") : new XybridDownload(boltffiHandle);
        }


        /// <summary>
        /// Current snapshot. Never blocks — safe from a UI thread or a per-frame
        /// render loop.
        /// </summary>
        public global::XybridBolt.XybridDownloadStatus Status()
        {
            ThrowIfDisposed();
            FfiBuf boltffiResultBuffer = NativeMethods.NativeXybridDownloadStatus(this.Handle);
            try
            {
                WireReader resultReader = new WireReader(boltffiResultBuffer);
                return global::XybridBolt.XybridDownloadStatus.Decode(resultReader);
            }
            finally
            {
                NativeMethods.FreeBuf(boltffiResultBuffer);
            }
        }


        /// <summary>
        /// Whether the download reached a terminal state.
        /// </summary>
        public bool IsFinished()
        {
            ThrowIfDisposed();
            return NativeMethods.NativeXybridDownloadIsFinished(this.Handle);
        }


        /// <summary>
        /// The failure message once the download ended in `Failed` or
        /// `Cancelled`; null otherwise. The stream carries the terminal *state*,
        /// this carries the reason.
        /// </summary>
        public string? Error()
        {
            ThrowIfDisposed();
            FfiBuf boltffiResultBuffer = NativeMethods.NativeXybridDownloadError(this.Handle);
            try
            {
                WireReader resultReader = new WireReader(boltffiResultBuffer);
                return resultReader.ReadU8() == 0 ? default(string?) : resultReader.ReadString();
            }
            finally
            {
                NativeMethods.FreeBuf(boltffiResultBuffer);
            }
        }


        /// <summary>
        /// Ask the download to stop. Takes effect within one chunk read, discards
        /// the partial file, and moves the status to `Cancelled`. Idempotent, and
        /// a no-op once the download is terminal.
        /// </summary>
        public void Cancel()
        {
            ThrowIfDisposed();
            FfiStatus status = NativeMethods.NativeXybridDownloadCancel(this.Handle);
            if (status.code != 0)
            {
                throw new global::System.InvalidOperationException($"BoltFFI call failed with status code {status.code}");
            }
        }


        private ulong TakeHandle() => unchecked((ulong)(ulong)global::System.Threading.Interlocked.Exchange(ref handle, 0));

        private void ThrowIfDisposed()
        {
            if (global::System.Threading.Interlocked.Read(ref handle) == 0)
                throw new global::System.ObjectDisposedException(nameof(XybridDownload));
        }

        private void Release()
        {
            ulong released = unchecked((ulong)(ulong)global::System.Threading.Interlocked.Exchange(ref handle, 0));
            if (released != 0) NativeMethods.NativeXybridDownloadRelease(released);
        }

        ~XybridDownload() => Release();

        public void Dispose()
        {
            Release();
            global::System.GC.SuppressFinalize(this);
        }
    }
}