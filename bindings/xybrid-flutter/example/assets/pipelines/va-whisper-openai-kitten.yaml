# Voice Assistant: Whisper + OpenAI + Kitten TTS
# ASR (device) -> LLM (cloud) -> TTS (device)

name: va-whisper-openai-kitten

stages:
  - whisper-tiny
    # Complete version:
    # id: asr
    # model: whisper-tiny
    # version: "1.0"
    # target: device

  - model: gpt-4o-mini
    target: cloud
    provider: openai
    system_prompt: "You are a helpful voice assistant. Keep responses concise."
    max_tokens: 150
    temperature: 0.7
    # Complete version adds: id: llm

  - kitten-tts-nano
    # Complete version:
    # id: tts
    # model: kitten-tts-nano
    # version: "0.1"
    # target: device
