# Voice Assistant: Whisper + OpenAI + Kokoro
# ASR (device) -> LLM (cloud) -> TTS (device)

name: va-whisper-openai-kokoro

stages:
  - whisper-tiny
    # Complete version:
    # id: asr
    # model: whisper-tiny
    # version: "1.0"
    # target: device

  - model: gpt-4o-mini
    target: cloud
    provider: openai
    system_prompt: "You are a helpful voice assistant. Keep responses concise."
    max_tokens: 150
    temperature: 0.7
    # Complete version adds: id: llm

  - kokoro-82m
    # Complete version:
    # id: tts
    # model: kokoro-82m
    # version: "1.0"
    # target: device
