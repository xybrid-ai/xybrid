# Voice Assistant: Wav2Vec2 + OpenAI + Kitten TTS
# ASR (device) -> LLM (cloud) -> TTS (device)

name: va-wav2vec2-openai-kitten

stages:
  - wav2vec2-base-960h
    # Complete version:
    # id: asr
    # model: wav2vec2-base-960h
    # version: "1.0"
    # target: device

  - model: gpt-4o-mini
    target: cloud
    provider: openai
    system_prompt: "You are a helpful voice assistant. Keep responses concise."
    max_tokens: 150
    temperature: 0.7
    # Complete version adds: id: llm

  - kitten-tts-nano
    # Complete version:
    # id: tts
    # model: kitten-tts-nano
    # version: "0.1"
    # target: device
