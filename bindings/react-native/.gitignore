# Build outputs
lib/
node_modules/
*.tgz

# Gradle output for the Android library module (from the example build).
android/build/
android/.gradle/
.kotlin/

# Native inputs resolved at `pod install` time by ios/xybrid_natives.rb — the
# release XCFramework (or a local build) and, in this monorepo, the Swift SDK
# sources synced from bindings/apple. The npm tarball carries the Swift
# sources (staged by scripts/prepack.sh), never the XCFramework.
ios/Frameworks/
ios/XybridSwift/

# Staged from the repo root by scripts/prepack.sh.
LICENSE
